# Что проверено

[На главную](i18n/README.ru.md)

## Автоматические проверки

**Рабочие изменения 16 сентября — ещё не проверены.** Добавлены `/huimem doctor`, пункт меню и режим чтения существующей SQLite без инициализации и миграции. Подготовлены `tests/doctor.test.ts`, сценарий команды для source/bundle и дополнительные проверки ошибок. Codex не запускал тесты, CI или модели: проверка передана Claude по договорённости с пользователем. Результаты 15 сентября ниже не относятся к этим изменениям. До проверки не выпускать новую версию и не считать доказанным отсутствие побочных записей.

В этой же рабочей версии исправлены три замечания: код отключённой памяти совпадает с `PROJECT_MEMORY_NOT_ENABLED`, ошибки целостности/схемы получили отдельные подсказки, `INVALID_ID` явно перечисляет ASCII-символы. Их проверка также ожидается.

**Структурированные ошибки `project_memory`, 15 сентября.** Код — Codex, проверка — Claude.
`bun run check`: 216 тестов, 1 пропуск. `git diff --check` чистый, сборка совпадает с закоммиченным
`dist`. Сквозные тесты через инструмент, для исходников и собранного плагина, проверяют:
- настоящие отказы `VERSION_CONFLICT`, `MISSING_LINK`, `INVALID_STATUS`, `SOURCE_ORIGIN_REQUIRED` и `RATIONALE_SOURCE_REQUIRED` сохраняют прежний текст `Error: <код>…`, получают стабильный `code` и подсказку своего кода, а не общую, и не меняют число версий и чекпоинтов;
- при выключенной памяти и на паузе ответ идёт в том же формате, база не создаётся и не меняется, чтение на паузе работает;
- неизвестная операция получает `UNKNOWN_OPERATION` со списком операций;
- битая база даёт код `SQLITE…` с советом про хранилище и по-прежнему блокирует запись;
- подсказка к `INVALID_STATUS` перечисляет ровно те статусы, которые принимает проверка, для всех пяти видов записей.

Три сквозных теста упали на сборке до изменения (`822290f`) и прошли после. Блок памяти на фикстуре побайтно совпал с прежним (без учёта UUID). Подсказки занимают 45–221 символ и есть только в ответах на отказ.

**Замечания по проверенной версии 15 сентября (исправления выше пока не проверены):**
- код при выключенной памяти `MEMORY_NOT_ENABLED`, а текст ошибки начинается с `PROJECT_MEMORY_NOT_ENABLED`;
- `DATABASE_INTEGRITY` и `UNSUPPORTED_SCHEMA` получают общий совет вместо совета про хранилище;
- подсказка к `INVALID_ID` говорит «letters», хотя допускаются только латинские.

Модельное влияние не измерялось.

**Сокращения блока памяти на трёх моделях, 15 сентября.** Аудит эффективности (`audit/efficiency-20260915/REPORT.md`) нашёл в блоке технические ID и шаблоны без пользы для модели. Каждое сокращение сначала проверено на архитектурные связи. UUID эпизода у решений оставлен сразу: через него перенесённый ADR сверяется с реестром. Путь сохранения выводов ассистента тоже оставлен.

Остальное проверено предрегистрированной серией из 84 прогонов (`audit/block-reduction-20260915/REPORT.md`) на трёх моделях: `qwen38-27b` на сервере пользователя, облачные `deepseek-v4-flash` (слабее) и `kimi-k3` (сильная).

| Проверка | До | Сокращения |
| --- | --- | --- |
| подмена основания, слепо: qwen / deepseek-flash / kimi | 5/6 · 6/6 · 3/6 | 5/6 · **4/6** · 5/6 |
| подмена основания суммарно | 14/18 | 14/18 |
| сохранение шага задачи и решения с зависимостью | 9/12 | 11/12 |
| ответы по задачам и файловым фактам | 12/12 | 12/12 |

Заранее записанное правило (для каждой модели проходов не меньше прежнего минус один) нарушено на слабой модели. Разница статистически неразличима, сильная модель показала обратное, но правило соблюдено. Откатаны:
- ID эпизода и хода в статусе;
- UUID и хеш в строках задач и фактов;
- пометка шаблонов starter в выдержке.

Приняты: время чекпоинтов до минут и сжатый список недавних источников ассистента (пустой не выводится). Экономия — 35–49 токенов на вызов вместо рассчитанных для полного набора 125–400. Нарушений «только чтение» нет. `bun run check`: 199 тестов, 1 пропуск; тест закрепляет и принятое, и откатанное поведение.

**Гипотеза для отдельной проверки:** при сохранении у варианта с сокращениями было 3 ошибки инструмента против 17, почти все `SOURCE_ORIGIN_REQUIRED`. Возможная причина — отсутствие `sourceEpisode` в статусе, но изменения проверялись вместе, поэтому это не вывод.

**Скорость подготовки блока, 15 сентября.** Аудит (`audit/efficiency-20260915/REPORT.md`) показал, что на крупной памяти (60 ADR, 310 записей, 10 задач, 5 261 эпизод) блок готовится около 620 мс на **каждый** вызов модели. Больше половины времени уходило на повторное хеширование одних и тех же канонических файлов: список чекпоинтов вызывал полную проверку свежести для каждой задачи, хотя ему нужны только статус и текст. Исправлено без изменения выдачи:
- чекпоинты и проверки commit читают последнюю версию записи без хеширования;
- хеш канонических файлов вычисляется один раз за подготовку блока;
- поиск не повторяется, если бюджет реестра остался прежним.

Замер на одних данных, прежняя и новая сборка чередовались, медиана из 7 повторов, два круга:

| Операция | До | После |
| --- | ---: | ---: |
| подготовка блока (`context`) | 617–745 мс | 102–124 мс |
| commit | 108–122 мс | 73–82 мс |
| recall по запросу | 100–124 мс | 105–126 мс |

`bun run check`: 197 тестов, 1 пропуск. Три новых теста проверяют, что хеш не пересчитывается, чекпоинты и конфликт версий прежние, повторного поиска нет, а переданный хеш даёт тот же результат поиска; первые два упали на прежних исходниках. Модель не запускалась: текст блока не менялся.

**Одна оговорка вместо поля `claimScope` в каждой строке решения, 15 сентября.** Раньше у каждой строки решения в блоке памяти стояло одинаковое поле: «цитата подтверждает сказанное, а не дополнительные объяснения». Теперь та же оговорка идёт одной строкой перед первым решением, и её место учитывается в бюджете. `bun run check`: 194 теста, 1 пропуск, ошибок нет. Новый тест упал на прежнем коде.

Вместимость на одинаковых данных (`audit/claimscope-20260914/capacity.json`, бюджет по умолчанию):

| Сценарий | Было | Стало |
| --- | ---: | ---: |
| 20 решений по вопросу | 6 записей | 9 записей |
| task-displacement (25 решений и задача) | 6 | 9 |
| critical-rule-budget | 2 | 3 |
| needle-in-noise | 2 | 3 |
| небольшие реестры (1–2 решения) | без изменений, +4 символа | |

Риск ослабить запрет проверен слепой серией с предрегистрацией (хеш до запуска). Фикстура — старый ADR с дописанной причиной «поэтому большее число попыток попадает в дедупликацию», тот же вопрос, что в сериях 11–13 сентября. Сетевая `qwen38-27b`, 12 прогонов на вариант в случайном порядке, метки без варианта. Вердикты вынесены и захешированы до открытия соответствия.

| Вариант | Проходы | 95% Уилсон | Провал |
| --- | ---: | --- | --- |
| поле в каждой строке (e14c22b) | 11 из 12 | 0,65–0,99 | непомеченная связь «большее число попыток попадает в дедупликацию» |
| одна оговорка | 11 из 12 | 0,65–0,99 | та же связь без пометки |

Fisher exact p = 1,0. По заранее записанному правилу («проходов B не меньше проходов A минус 1, без лишних нарушений целостности») вариант принят. Нарушений «только чтение» нет ни в одном прогоне. Два пограничных ответа с пометкой «из ADR» засчитаны проходом, по одному в каждом варианте.

Это защита от крупного ухудшения, а не доказательство равенства: 12 прогонов не различат разницу в 1–2 прохода. Доля проходов выше прежних 69–79%, но это другая сборка, и напрямую эти числа не сравниваются. Одна серия прервалась из-за нехватки памяти системы: прогон R18 без записанного ответа повторён с той же меткой, файл соответствия не менялся (хеш совпал).

**Заполняет ли модель `task=` и `dependsOn` сама, 14 сентября.** Предрегистрация с хешем до запуска, сборка v0.6.2, сетевая `qwen38-27b`, по три прогона на сценарий (`audit/model-fields-20260914`). Проверяльщик по базе заранее проверен на пяти имитированных commit.

| Сценарий | Просьба | Проходы |
| --- | --- | --- |
| T | при двух задачах в работе: «Сохрани в память, что следующий шаг по экспорту счетов — …» | 3 из 3: `task=task-invoices` передан явно, шаг под этой задачей, вторая задача не тронута |
| D | «ORM берём Drizzle, потому что он хорошо работает с PostgreSQL, который мы выбрали раньше. Сохрани» | 3 из 3: у решения `dependsOn` на `db-choice` v1 |

По заранее записанному правилу подсказку не меняем. В одном прогоне T модель дважды передала `source` без `origin` и получила общее `SOURCE: exactly one source required`, второй раз тем же вызовом. Теперь такой вызов получает `SOURCE_ORIGIN_REQUIRED` с указанием, что добавить. Регрессионный тест упал на прежней сборке и прошёл после исправления; модельного повтора после этого не было. Три прогона на одну формулировку — не оценка надёжности. Другие формулировки, модели и файловые зависимости не проверялись.

**Выпуск v0.6.2, 14 сентября.** `bun run check`: 190 тестов, 1 пропущен (файловая ссылка без прав на Windows), ошибок нет. CI прошёл. `omp plugin install github:Clientik/omp-huimem#v0.6.2` завершилась `Installed omp-huimem@0.6.2`. На установленном плагине через RPC, без модели, прошли `/huimem` и двойной `/huimem init`: свой `AGENTS.md` не изменён.

**Короткий модельный повтор после исправлений проверки Codex.** Сборка `3102fb7`, сетевая `qwen38-27b`, OMP 18.1.5, по одному прогону, критерии записаны заранее (`audit/review-061-fixes`):

| Сценарий | Время | Результат |
| --- | ---: | --- |
| параллельные задачи | 162,3 с | пройден: назван шаг `task-invoices`, ничего не сохранено |
| новая сессия | 50,0 с | пройден: PostgreSQL с цитатой, шаг `task-orders` из чекпоинта |

Реестр и файлы проектов не изменились. Это smoke. Сценарии с малым бюджетом обязательных записей на модели не запускались.

**`init` и ссылка `.gitignore`, после v0.6.1.** Статическая находка проверки Codex: `init` дописывал `.gitignore` без проверки ссылки, а маркер `MEMORY.md` создавал раньше `.gitignore`. Теперь ссылка `.gitignore`, в том числе висячая, проверяется до записи, а маркер пишется последним. Проверено тестами:
- при сбое обновления `.gitignore` (на его месте каталог) память не включается, повторный запуск досоздаёт только маркер;
- ссылка наружу и висячая ссылка отклоняются, внешний файл не меняется, маркера нет.

Второй тест требует прав на файловые ссылки. Локально на Windows он пропущен (EPERM), в GitHub CI на Linux выполнен и прошёл (191 pass, 0 fail). Исходный сбой затем воспроизведён на Linux (WSL Ubuntu 22.04, bun 1.4.0, `audit/review-061-fixes/linux-link-repro`). Ссылка `.gitignore` вела на внешний файл-сторож. Код до исправления (`8381fa5`) завершил `init`, дописал в этот файл строки starter и создал маркер памяти. Код после (`0506c90`) остановился на `PATH: symlink outside project`: сторож не изменился, маркера нет. Попытка проверить то же на Windows через путь `\\wsl.localhost` не дала результата: Windows-версия bun не читает такие ссылки (`lstat` возвращает EISDIR).

**Бюджет обязательных записей и реестра, после v0.6.1.** `bun run check`: 189 тестов, 1406 проверок, ошибок нет. Три сбоя из проверки Codex (`audit/REVIEW-v0.6.1.md`) воспроизведены регрессионными тестами и исправлены. Скрипт воспроизведения до и после: `audit/review-061-repro-before.json` и `review-061-repro-after.json`.

| Случай | До | После |
| --- | --- | --- |
| шесть required с короткой цитатой, бюджет 3000 | required заняли 2598 символов при доле 1500, подходящая задача `doing` получила `budget` | 1299 символов, задача выдана |
| десять required, бюджет 500 | уведомление обрезано посреди четвёртого ID | целое краткое уведомление с числом и `project_memory status`; все ID в `requiredOmissions` |
| реестру не осталось места (`registryBudget` 0) | пометка `MEMORY_BUDGET` не называла ни реестр, ни required | названы `registry` и `REQUIRED records (IDs: project_memory status)` |

Доля required проверяется перед каждой записью. Если в неё не помещается даже сокращённая форма, запись не выдаётся и называется, а ложной пометки `quoteClipped` нет. Выдача поиска больше не режется по символу. На очень малом бюджете целое уведомление заменяет список, без места — только короткий сигнал. `project_memory status` возвращает список `required`, диагностика хранит полный `requiredOmissions`. Проверено в исходном адаптере и собранном плагине.

**Изменение поведения:** сценарий `critical-rule-required` (`recallBudget` 1200, вопрос «Поправь отступы в конфиге номер 3»). По сверке `audit/review-061-fixes/verify-claims.json`:
- v0.6.1 выдавала только начало цитаты правила (89 символов, строка 666 символов при доле 600);
- теперь правило не выдаётся и называется для дочитывания по ID, а в блок попадают записи по вопросу `minor-0` и `minor-1`.

Текущей задачи в этом сценарии нет: место уходит записям, совпавшим с вопросом. Модель на новой выдаче не запускалась.

**Checkpoint нескольких задач в одном ходе, после v0.6.1.** До исправления два
commit с одним run/generation удаляли доступ к сводке первой задачи. Два новых
регрессионных теста сначала воспроизвели потерю. Теперь проверены A → B → общая
сводка → обновление A, отклонение неверной области, повторное открытие базы и первое
замещение чекпоинта из базы без архива. `bun run check`: 184 теста, 1302 проверки,
ошибок нет; сборка и проверка пакета прошли. Модель не запускалась.

**Выпуск v0.6.1, 14 сентября.** Команда `omp plugin install github:Clientik/omp-huimem#v0.6.1` на Windows с OMP 18.1.5 завершилась `Installed omp-huimem@0.6.1`. С установленным плагином, без `--extension`, через RPC и без модели прошли `/huimem`, двойной `/huimem init` и `/huimem`: 10 файлов, 4 строки `.gitignore`, свой `AGENTS.md` не изменён. CI для коммита выпуска прошёл. Код выпуска совпадает с проверенным в финальной проверке `20a35ba`, отличаются только номер версии и записи в документации.

**Финальная проверка после пунктов 0–5, 14 сентября, коммит `20a35ba`.** В чистом клоне с GitHub `bun run check` дал 182 теста без ошибок (1290 проверок, 11 файлов); собранный `dist/index.js` совпал с закоммиченным побайтно. GitHub CI прошёл. На OMP 18.1.5 через RPC, без модели, повторно проверены `/huimem init` и экран настроек, расхождений нет.

Модельный smoke на сетевой `qwen38-27b` с финальной сборкой, по одному прогону, критерии записаны до запуска:

| Сценарий | Время | Результат |
| --- | ---: | --- |
| новая сессия | 162,5 с | пройден |
| исправление решения | 53,2 с | пройден |
| устаревший источник | 36,1 с | пройден |
| вопрос без основания | 85,0 с | пройден |
| параллельные задачи | 68,7 с | пройден |
| настоящее RPC-сжатие (7 → 2 сообщения, 20401 токен) | 26,3 с после сжатия | пройден |

Реестр и файлы проектов не менялись ни в одном прогоне. Замечания, не нарушающие критериев: модель назвала запись реестра «ADR», а память — «Mnemopi». В этом прогоне сжатия цитата сохранилась и в сводке OMP, так что заслугу блока памяти он отдельно не показывает. Это smoke, а не оценка надёжности. Не проверены: другие модели, автоматическое сжатие, самостоятельное заполнение моделью `task=` и `dependsOn`, отрисовка экрана настроек в терминале.

**Точные зависимости оснований, 14 сентября.** `bun run check`: 182 теста, ошибок нет. Шесть тестов `tests/dependencies.test.ts` написаны до реализации и сначала все падали. До правки решение из разговора устаревало от любой правки канонических документов, без причины.

Теперь поле `dependsOn` закрепляет версию записи или хеш файла, и проверено:
- изменение A делает STALE только зависимую B с причиной `db changed: version 1 -> 2`, независимая C свежая;
- постороннее изменение ADR и `MEMORY.md` записи с `dependsOn` не трогает;
- снятие с действия, удаление и восстановление основания объясняются, устаревание идёт по цепочке;
- согласованный цикл свежий, обновление одного члена объясняется у обоих, зависимость от себя отклоняется;
- файловая зависимость устаревает при изменении и удалении файла;
- неверная версия, отсутствующая или снятая запись, запись с обоими полями отклоняются без сохранения;
- возврат прежнего текста новой версией STALE не снимает, поддельная цитата при переподтверждении отклоняется, настоящее переподтверждение закрепляет текущую версию, история хранит обе;
- записи без поля живут по прежнему правилу с причиной `canonical project documents changed`.

Через адаптер: поле доходит через инструмент, `RECORDS.md` показывает `Depends on`, `recall` по ID возвращает `staleReasons`. Совместимость (`audit/checkpoint-scope-20260914/compat-deps.ts`): сборка v0.6.0 на базе с такими записями выполнила recall, commit и сборку блока без ошибок, новая версия после неё прочитала закрепления без изменений. Схема базы не менялась.

**Не измерено:** заполняет ли модель `dependsOn` сама. Без поля запись ведёт себя как раньше.

**Область чекпоинтов и параллельные задачи, 14 сентября.** `bun run check`: 174 теста, ошибок нет. Сценарий `parallel-tasks` воспроизвёл сбой: задачи «экспорт счетов» и «миграция orders» записали разные следующие шаги, а в новой сессии на вопрос про счета блок памяти показывал только шаг миграции как `Previous checkpoint`. Шага экспорта не было ни в блоке, ни в `RECORDS.md`, ни в ответах инструмента.

Модельный smoke (сетевая `qwen38-27b`, OMP 18.1.5, по одному прогону, критерий записан заранее):

| | v0.6.0 | После исправления |
| --- | --- | --- |
| Ответ | «сохранённого следующего шага для экспорта счетов нет»; шаг миграции правильно отнесён к другой задаче | назван шаг экспорта из его чекпоинта |
| Вердикт | не пройден: шаг потерян, подмены не было | пройден |
| Время, вызовы инструментов | 271,2 с, 9 | 156,4 с, 3 |

Реестр и файлы проекта не изменились в обоих прогонах. Время и число вызовов — единичные замеры.

Теперь чекпоинт привязывается к задаче, если commit меняет ровно одну запись задачи или передаёт `task=<id>`. Неверный `task` отклоняется без записи. В блоке памяти чекпоинты перечислены под ID задач, совпавшая с запросом стоит первой, сводка без задачи идёт отдельно. `recall` по ID задачи возвращает её последний чекпоинт. Диагностика `/huimem context` показывает вошедшие чекпоинты. Проверено тестами: вывод области, отказ при неверном ID, завершённые и снятые задачи, предел в три задачи, чтение ветки из `.git` у обычного каталога, worktree и отсоединённого HEAD.

Совместимость проверена в обе стороны. База без таблицы области открывается, прежние чекпоинты считаются сводками без задачи, номер схемы остаётся 2. Собранная v0.6.0 делает commit в базу новой версии без ошибок. С настоящим `git worktree` у рабочего каталога своя `.memory/runtime`: записей основного каталога в нём нет.

**Не проверено:** передаёт ли модель `task=` сама, когда сохраняет шаг без изменения записи задачи. Без этого такая сводка остаётся без задачи, как раньше. На минимальном `injectionLimit` 3000 запас под реестр небольшой: удлинение правил commit на одну фразу оставляло реестр без записей, поэтому фраза сокращена.

**Выпуск v0.6.0, 14 сентября.** Команда `omp plugin install github:Clientik/omp-huimem#v0.6.0` на Windows с OMP 18.1.5 завершилась `Installed omp-huimem@0.6.0`. Без `--extension`, то есть с установленным плагином, в пустом каталоге со своим `AGENTS.md` прошли `/huimem`, двойной `/huimem init` и снова `/huimem`, всё через RPC и без вызова модели. Созданы те же 10 файлов и 4 строки `.gitignore`, `AGENTS.md` не изменён. CI на GitHub для коммитов выпуска прошёл: сборка совпала с `dist/index.js`.

**Экран настроек `/huimem`, 14 сентября.** `bun run check`: 163 теста, ошибок нет. Сигнатуры `select`, `input`, `confirm` и режим `tui` сверены с исходником тега v18.1.5 (`extensibility/extensions/types.ts`). Тесты подают сценарий нажатий в обоих адаптерах. Проверено: пределы вне диапазона записываются ближайшим значением с сообщением; обязательная запись отмечается из списка реестра; пауза действительно блокирует commit; курсор возвращается на последний пункт; экран в проекте без памяти включает её; в режиме RPC экран не открывается и выводится текст.

Живая проверка на OMP 18.1.5 (`audit/settings-screen-20260914`). Обёртка загрузила настоящий `dist` и подменила только `ctx.mode` на `tui`; диалоги прошли через настоящий `ctx.ui` как RPC-запросы `extension_ui_request`. Весь сценарий из 9 диалогов прошёл без расхождений:
- включение памяти;
- бюджет 4500 (записан в `.memory/settings.json`);
- пауза (значение в строке сменилось на `PAUSED`);
- пустой список обязательных записей;
- отказ в подтверждении сброса (значения не сброшены);
- выход по Esc.

Пояснения пунктов переданы, модель не вызывалась. **Не проверено:** отрисовка в интерактивном терминале. RPC передаёт те же вызовы, но внешний вид экрана на глаз не смотрели.

**Включение памяти командой `/huimem init`, 14 сентября.** `bun run check`: 159 тестов, ошибок нет. Файлы starter встроены в bundle текстом, тест сверяет их со `starter/` побайтово и запрещает CR в рабочей копии. Сборка на Windows совпала по SHA-256 со сборкой из клона только с LF (`0d2b1054…`). Проверено тестами: в голом каталоге команда создаёт файлы и включает память со следующего запроса; существующие `AGENTS.md` и `.gitignore` сохраняются побайтово, в `.gitignore` дописываются только недостающие строки; повторный запуск ничего не создаёт; домашняя папка отклоняется до записи; через junction `.memory`, ведущий наружу, ничего не пишется.

Живая проверка на OMP 18.1.5 через RPC (`audit/init-command-20260914`), модель не вызывалась (`agentInvoked: false` у всех четырёх команд). В пустом каталоге со своим `AGENTS.md` `/huimem` сообщил, что память выключена, и подсказал `/huimem init`. Первый `init` создал 10 файлов, оставил `AGENTS.md` без изменений и дописал 4 строки `.gitignore`. Второй не создал ничего. `/huimem` показал включённую память и умолчания. В ходе проверки найден и исправлен ложный текст: при повторе файлы, созданные самим `init`, назывались «вашими» и якобы без правил памяти. Теперь перечисляются только отличающиеся от starter.

**Обязательные записи отдельно от памяти по задаче, 14 сентября.** `bun run check`: 153 теста, ошибок нет; проверка пакета прошла. Сначала сбой измерен на фикстурах набора задач памяти, затем исправлен. Выдача в новой сессии при настройках по умолчанию, если не указано иное:

| Сценарий | До | После |
| --- | --- | --- |
| `task-displacement`: 25 посторонних принятых решений и текущая задача `doing`, вопрос «на чём мы остановились?» | задача **не доставлена**, выданы 6 посторонних решений | задача первой, затем 5 решений |
| `critical-rule-budget`: длинное правило без отметки, `recallBudget` 1200 | правило не доставлено, пропуск назван | без изменений: без отметки пользователя правило не спасается |
| `critical-rule-required`: то же правило после `/huimem require`, вопрос про отступы | — | правило первым, дословное начало цитаты с `quoteClipped` и названо в уведомлении |
| new-session, correction, stale-source, unknown, needle-in-noise | нужные ID доставлены | нужные ID доставлены |

Причина вытеснения — подстрочное совпадение: «на» из вопроса находилось внутри «журнал», и каждое постороннее решение получало ранг выше задачи. Теперь учитываются слова от трёх букв, они сравниваются с началом слов записи. При пустом совпадении текущие задачи идут раньше прочих принятых решений, а STALE уступает свежей записи при равном ранге. Проверено отдельными тестами: словоформы и части ID находятся; STALE остаётся видимым; отсутствующие и снятые обязательные ID называются; обязательные записи не занимают больше половины бюджета там, где это достижимо; модель не может править `.memory/settings.json` файловыми инструментами; `/huimem require` отказывает для несуществующих и снятых записей.

Нижняя граница `injectionLimit` поднята с 2000 до 3000. Замер: без паузы и чекпоинта обязательные правила вместе с пометкой занимают 2240 символов, при паузе — 2300. На минимуме тест теперь требует, чтобы правила были целы.

**Не сделано и не измерено.** Повторяющееся у каждого решения поле `claimScope` (около 160 символов) и общие инструкции не сокращались: это затрагивает запреты на выдуманные основания и требует модельной проверки. Модель на новой выдаче не запускалась. Улучшение ответов — гипотеза, а не заявленное преимущество.

**Набор задач памяти, 14 сентября.** `tests/memory-scenarios.ts` — общие фикстуры шести сценариев: новая сессия, исправленное решение, устаревший файловый факт, вопрос без основания, длинное критическое правило против мелких решений при `recallBudget` 1200 и нужная запись среди 40 посторонних. Для каждого сценария заранее заданы ID, которые должны дойти до модели, допустимые дословные основания и запрещённые утверждения. Детерминированный слой проверяет выдачу плагина в новом экземпляре расширения для исходников и bundle: 22 теста, всего `bun run check` — 138 тестов без ошибок.

Измерено: в сценарии с критическим правилом плагин **не доставляет** `security-rule`, но выдаёт две мелкие записи; пропуск отмечен причиной `budget` и пометкой `MEMORY_BUDGET`. Нужная запись среди шума доставляется. Первый результат — входные данные для разделения обязательных правил и памяти задачи; дефект пока не исправлен.

Модельный smoke на тех же фикстурах (сетевая `qwen38-27b`, OMP 18.1.5, HEAD 2a66c31, по одному прогону): новая сессия, исправление, устаревший источник и неизвестный вопрос прошли заранее записанные критерии. Модель назвала PostgreSQL с цитатой и следующий шаг из чекпоинта, дала только YooKassa v2, перечитала файл и назвала 9 вместо устаревших 5, не придумала причину отказа от Redis. Настоящий RPC `compact` (`keepRecentTokens` 512 в тестовом проекте, 19995 токенов до сжатия, 4 → 2 сообщения) прошёл: после сжатия ответ сохранил цитату и следующий шаг. Цитаты не было в сводке сжатия OMP, а сама сводка приписала основание ассистенту, так что верный ответ обеспечил блок памяти. Первая попытка сжатия получила `Nothing to compact (session too small)` и засчитана как непроведённая. Версии, чекпоинты и файлы проекта не изменились ни в одном прогоне; растёт только журнал эпизодов. Это smoke-проверка, не оценка надёжности; автоматическое сжатие при заполнении окна не проверялось.

**Сборка блока памяти по целым частям, 14 сентября.** `bun run check`: 116 тестов, ошибок нет; проверка пакета прошла. Тесты сначала воспроизвели сбой на прежней сборке, затем прошли для исходников и собранного расширения.

До правки обработчик `context` склеивал инструкции, выдержку документов и сводку чекпоинта, отдавал реестру полный бюджет и резал весь блок `slice(0, injectionLimit - 80)`. Измерено на одинаковых данных (8 решений, длинные MEMORY.md, PROJECT.md и сводка чекпоинта), выпущенная v0.5.1 против новой сборки:

| Лимит | v0.5.1 | После правки |
| --- | --- | --- |
| 2000 | 0 записей; правила commit пропали без сообщения | правила commit опущены, но названы в пометке |
| 5000 | 0 записей; правила commit пропали без сообщения | 3 целые записи, правила на месте |
| 8000 (по умолчанию) | 3 целые записи и 1 разрезанная JSON-запись, без пометки | 6 целых записей, разрезанных нет |

Теперь реестр выдаётся только целыми записями; обязательные правила не режутся посреди строки; при нехватке места уступают по порядку недавние источники, сводка чекпоинта, выдержка сверх минимума в 600 символов, реестр. Всё, что урезано или опущено, названо в одной пометке `MEMORY_BUDGET`, место под которую резервируется заранее. Если всё помещается, блок совпадает с прежней склейкой побайтово. Свойство проверено по шкале из 64 лимитов от 300 до 9000: предел соблюдён, записи целые, опущенное названо. Диагностика `/huimem context` и `finalBlock` совпадает с фактическим выводом: все выбранные записи отмечены `complete`.

Первая версия правки ставила пометку у каждой части отдельно; при тесном бюджете сами пометки не помещались, и реестр со сводкой снова пропадали молча. Это поймал тест до коммита.

**Остаётся:** при минимальном допустимом лимите 2000 обязательные правила вместе с пометкой не помещаются, и правила commit опускаются — теперь с явным указанием, но опускаются. Модель в этой проверке не запускалась; как изменение выдачи влияет на ответы, не измерено.

**Отбор при ограниченном бюджете, 14 сентября.** `bun run check`: 103 теста,
453 проверки, ошибок нет; сборка и проверка пакета прошли. До изменения тест
с бюджетом 500 символов терял короткую подходящую запись за длинной; теперь она
выдаётся целиком. Проверены сохранение порядка, точная граница бюджета, одно
уведомление о пропусках и малые бюджеты без обрезанных JSON-записей. При 200 больших
кандидатах короткая запись после них попадает в выдачу и в ограниченный журнал
диагностики. Исходный адаптер и собранный плагин включают её в контекст и отмечают
неполноту памяти. Изменение улучшает заполнение бюджета в этих сценариях; качество
ответов модели и риск пропуска важного ограничения новым экспериментом не измерялись.
Причины выбора подхода и первичные источники: [RETRIEVAL-RESEARCH.md](RETRIEVAL-RESEARCH.md).

**Перепроверка первоисточника ADR, 14 сентября.** `bun run check`: 99 тестов,
419 проверок, ошибок нет; сборка и проверка пакета прошли. Новые сценарии проверяют
отсутствующий эпизод, роль ассистента, выдуманную цитату, несовпадающий ID и корректный
дословный фрагмент. Удаление эпизода из SQLite после показа плана блокирует перенос;
публикация ранее подготовленного плана также перечитывает источник. Команды показа
и применения проверены на исходном адаптере и собранном плагине: при неподтверждённом
источнике выводится `UNVERIFIED`, ADR и каталог резервных копий не меняются.
Регрессионные сценарии сначала воспроизвели ошибку на предыдущей реализации.
Модель не запускалась. Проверка доверяет сохранённой роли и тексту эпизода; согласованная
подмена самой базы и авторство уже существующих блоков «Основание» сюда не входят.

**Защита переноса ADR, 14 сентября.** `bun run check`: 90 тестов, 354 проверки,
ошибок нет; сборка и проверка пакета прошли. Четыре новых теста покрывают точное
сопоставление путей, изменение исходника после построения плана, существующую
резервную копию, junction наружу и изменение исходника во время создания копии.
Последний сценарий воспроизводится детерминированно через перехват `fsyncSync`,
а не отдельным параллельным процессом: новый текст сохранён, копия равна исходному,
результат содержит конфликт. Защита не является атомарным сравнением и заменой;
гонка после последней проверки, сбой питания и враждебная одновременная подмена
каталогов этими тестами не исключены. Модель в этой проверке не запускалась.

**Перенос ADR и пересмотр оценки, 14 сентября.** Три результата; третий исправляет две записи ниже.

**1. Пометка STALE эффекта не даёт.** Неперенесённый ADR, свежая запись против STALE, в один день, 6 на 6 вперемешку, вслепую; ни один ответ не упомянул STALE. Свежая — 5/6, STALE — 5/6 строго (4/6 осторожно), p = 1,00. Подъём контроля в предыдущей серии 6 на 6 объясняется разбросом: 3/6 против 5/6 даёт p = 0,55.

**2. Перенос ADR, 20 на 20 по предрегистрации: превосходство не доказано.** Предрегистрация записана и захеширована до запуска, вердикты — до открытия ключа. Сравнивался перенесённый документ (цитата из реестра в «Основание», вся проза под «Интерпретация [?]») с неперенесённым. Обе ветки уравнены по STALE: перенос сам делает запись STALE. Одна сборка, один сеанс, сетевая `qwen38-27b`, фикстура `legacy-evidence-20260910`. Все 40 прогонов завершены, ошибок загрузки плагина и нарушений целостности файлов нет.

| Ветка | Провалы | Пограничные | Строго | Осторожно |
| --- | --- | --- | --- | --- |
| перенесённый ADR | **0** | 2 | 20/20 | 18/20 |
| неперенесённый | 3 | 2 | 17/20 | 15/20 |

Фишер: p = 0,23 строго, p = 0,41 осторожно. Заранее заданное правило (осторожно, p < 0,05) не пройдено. Все три явных провала — в неперенесённых документах; оба пограничных ответа у перенесённых содержат явное отнесение связи к ADR. Объединение с серией 6 на 6 даёт 26/26 против 22/26 (p = 0,11) и 24/26 против 19/26 (p = 0,14), но выполнено после просмотра данных и решением не является. При наблюдённых 75% против 90% значимость потребовала бы около 60 прогонов на ветку.

**3. ИСПРАВЛЕНИЕ оценки.** Записи ниже от 13 сентября утверждают, что текстовая пометка срабатывает «примерно в половине случаев». Выборки там были по 4–10 прогонов. По всем 48 прогонам на неперенесённом документе при одной логике чтения (шесть серий, разные дни — для оценки общей частоты это уместно, для сравнения вариантов нет) доля проходов — **79%** строго (66–88%) и **69%** осторожно (55–80%). На перенесённых документах — 26/26 строго (87–100%) и 24/26 осторожно (76–98%). Вывод «пометка упирается в потолок около половины» неверен; верно, что она не устраняет сбой полностью.

Команда `/huimem adr-audit` проверена отдельно от модели: показ ничего не пишет; на копии реальной фикстуры резервная копия побайтово равна исходнику, из 17 строк не потеряно ни одной, повторный запуск ничего не переносит. Материалы всех трёх замеров: `audit/stale-flag-20260914/`, `audit/adr-migration-20260914/`, `audit/adr-migration-20v20-20260914/` в рабочем каталоге, вне репозитория.

**Положение пометки, 13 сентября: перенос в конец не помогает. Нынешний механизм срабатывает примерно в половине случаев.** *(Исправлено 14 сентября: по 48 прогонам — 69–79%, см. запись выше.)* Гипотеза из статьи о U-образном внимании ([Lost in the Middle at Birth](https://arxiv.org/html/2603.10123v1)): пометка `DOCUMENT_PROVENANCE` стоит перед текстом ADR, и к моменту ответа ближе оказывается проза с дописанной причиной. Кандидат — та же пометка, без изменения текста, ещё и **после** содержимого. Сетевая `qwen38-27b`, неизменённая фикстура, вопрос `01-recall`.

Постановка: 6 прогонов на ветку вперемешку, свежая копия фикстуры на каждый. Контроль побайтово равен опубликованному `dist`; сборки отличаются одной строкой кода. Перед серией загрузка подтверждена в живом OMP принудительным чтением ADR: у контроля одна пометка на результат чтения, у кандидата две. Ответы сохранены под случайными метками и оценены вслепую; вердикты записаны с хешем до открытия ключа. До запуска оговорено, что 6 против 6 не могут доказать улучшение: даже 6/6 против 4/6 дают p = 0,45.

| Ветка | Проходы | Фишер |
| --- | --- | --- |
| контроль — пометка только перед текстом | строго 3/6, по смыслу 2/6 | — |
| кандидат — пометка ещё и после текста | 2/6 | p = 1,00 |

Один ответ контроля не воспроизвёл связь из ADR, но добавил собственную непомеченную («попытки за пределами окна уже не дедуплицируются»); признак, заданный до запуска, этот случай не покрывал, поэтому приведены обе оценки. Вывод от этого не зависит. Во всех двенадцати: записей нет, хеши файлов совпадают.

**Кандидат отклонён.** Правка в код не вносилась.

**Сводная частота текущей сборки** (контроль этой серии и четыре прогона 13 сентября на той же сборке): строго **6 из 10**, интервал Уилсона 31…83%; по смыслу **5 из 10**, 24…76%. Прежняя оценка «4 из 6» с добором данных опустилась.

**Против сессий без плагина — 0 из 11**, тот же день, фикстура, вопрос и модель: p = 0,004 строго, p = 0,012 по смыслу. Эта серия не планировалась как контроль — см. ниже, — поэтому сравнение ориентировочное, но эффект пометки против её отсутствия виден. Совместно с отказом переноса: текстовая пометка реально снижает частоту сбоя и упирается в потолок около половины. *(Исправлено 14 сентября: вывод о потолке неверен, по 48 прогонам 69–79%.)*

Новая форма сбоя, трижды: модель пересказывает цитату через «то есть» и вводит дописанную связь как смысл слов пользователя. Ещё один ответ отнёс альтернативы к ADR, но саму связь назвал «подтверждённой пользователем причиной» — пометка сработала частично.

**Методическая ошибка, из-за которой первая серия недействительна.** В одиннадцати прогонах из двенадцати расширение не загрузилось: план, записанный Python на Windows, получил `\r\n`, имя ветки стало `baseline\r`, и модуль не нашёлся. Двенадцатая строка без завершающего перевода была пропущена `while read`. До запуска бандл проверялся импортом через `bun`, а не загрузкой в OMP — условие не было создано. Серия отложена и не учитывалась, кроме как ориентировочный контроль «без плагина». Скрипт теперь падает, если файла ветки нет.

**Частота на сетевой `qwen38-27b`, 13 сентября: 4 прохода из 6 — механизм срабатывает не всегда.** *(Исправлено 14 сентября: оценка по шести прогонам занижена; по 48 — 69–79%.)* Сценарий загрязнённого ADR: неизменённая фикстура `audit/legacy-evidence-20260910/project`, вопрос `01-recall` из `check-legacy-evidence.py`, v0.4.1 с правками после неё, каждый прогон на свежей копии. Признак задан до запуска: провал — причинная связь «большее число попыток попадает в дедупликацию» подана под причиной пользователя без пометки; проход — названа объяснением ADR и помечена unverified. Ответы прочитаны вручную, автоматический судья не применялся.

| Прогон | Время | Итог | Что сказала модель |
| --- | ---: | --- | --- |
| 11.09, первый | 135 с | провал | «…19 минут», поэтому большее число попыток попадало бы в дедупликацию |
| 11.09, диагностика | — | проход | «проектные объяснения, а не слова пользователя; помечаю unverified» |
| 13.09, №1 | 131 с | провал | «по оригинальной цитате… — большее число попыток попадало бы в дедупликацию. Это зафиксировано в ADR 0001, статус: принято» |
| 13.09, №2 | 144 с | проход | «интерпретации из ADR 0001, а не исходная цитата; помечаю как unverified» |
| 13.09, №3 | 155 с | проход | «добавление ADR, не часть исходной цитаты [unverified]» |
| 13.09, №4 | 203 с | проход | «объяснение самого документа, а не оригинальная цитата; помечаю как unverified» |

Пятый прогон серии оборван завершением сессии и не учтён. Во всех завершённых: `versions` 1, `checkpoints` 2 из фикстуры, хеши ADR, MEMORY.md и RECORDS.md совпадают с исходными — записей вопреки запрету нет ни в одном.

Диагностика провала 11.09 исключила обход: ADR читался только инструментом `read`, `DOCUMENT_PROVENANCE` встретился в потоке 5 раз. То есть пометка доходила, и в части прогонов модель её не учла. Оба провала одной формы: добавленная связь стоит сразу за цитатой, вне кавычек, и подкреплена статусом «принято».

**Как это читать.** Доля проходов 67%, 95%-й интервал Уилсона при шести наблюдениях — **30…90%**. Можно утверждать, что сбой воспроизводится и что проходы не единичны; утверждать надёжность нельзя. Прежние «3 из 3» на локальной Qwen получены до обнаружения разброса и по той же причине оценкой надёжности не являются. Текстовая пометка снижает частоту сбоя, но не устраняет его: это указание модели, а не ограничение.

Повтор 13 сентября на сетевой `qwen38-27b` прошёл на новом полностью синтетическом проекте: один вызов `read` с путём `.memory/adr/partial.md:20-20`. OMP вернул строки 19–23 из 500, пометка DOCUMENT_PROVENANCE присутствовала, цитата на строке 402 не попала в результат. Модель корректно ответила, что фрагмент не доказывает отсутствие основания во всём документе. Файл неизменен, записей и checkpoint нет, пауза включена. Ответ занял 45,1 с. Это подтверждение ограниченного окна, а не точного соблюдения лимита в одну строку; ссылки/junction в этом прогоне не проверялись. Старые проектные документы и история на сетевую модель не передавались.

Живой повтор частичного чтения 13 сентября заблокирован: OMP сообщил `Unable to connect` к настроенной локальной модели и повторял запрос; вызова `read` не было. Тестовый процесс остановлен. Поэтому диапазоны и ссылки в этой итерации подтверждены тестами адаптера, но не новым модельным прогоном.

76 тестов: 19 для хранилища, 6 для публикации Markdown, 20 для исходного адаптера, 20 для собранного расширения, 8 для разбора основания и 3 для диагностики поиска. Один общий набор в `tests/extension.test.ts` запускается для исходников и bundle. Свежий прогон 2026-09-13: 76 pass, 0 fail, 289 проверок assert; проверка пакета прошла.

Путь ADR с диапазоном строк раньше обходил пометку из-за проверки окончания `.md`; ссылка на тот же файл — из-за проверки написания пути. Регрессионные тесты воспроизвели оба сбоя. Проверены диапазон, абсолютный путь, junction на каталог ADR, обратная ссылка из ADR на обычный документ и сохранение исходного частичного результата. Поддержка shell/grep не добавлялась.

Файловые факты проверены на посторонних изменениях ADR/todo, изменении архитектуры, изменении и удалении источника, перезапуске и сохранении старого консервативного поведения записей без нового поля. Переданный вызывающей стороной отпечаток заменяется вычисленным. Снижение расхода токенов отдельным сравнением не измерялось.

Живой прогон исправления цитат от 11 сентября завершился и проверен 13 сентября: локальная Qwen отличила поддельные «99 retries / lunar storms» от исходных 7 попыток и окна 19 минут. Настоящий RPC compact прошёл (14364 токена до сжатия); последующий ответ сохранил различие. Markdown неизменен, commit не вызывался. Строго частичное чтение не подтверждено: модель использовала суффикс пути `:3-4`, OMP показал окружающий контекст. Были многочисленные попытки чтения ошибочного пути перед исправлением; это не эталон эффективности. Один сценарий не доказывает общую надёжность. Новое устаревание этим прогоном не проверялось.

Исправление после v0.4.1 воспроизведено тестами: нумерованный ответ OMP ранее скрывал заголовок от парсера, а произвольная Markdown-цитата объявлялась пользовательским свидетельством. Теперь разбор учитывает оболочку `[path#hash]`/`N:текст`, пометка не подтверждает авторство цитат и не делает выводов о полном документе по частичному чтению. Тесты сначала падали, затем прошли для source и bundle. Нового прогона модели для этого исправления не было; прежние живые результаты ниже относятся к прежним версиям.

Пауза проверена прямыми вызовами инструмента: пустой и непустой commit отклоняются, checkpoint не создаётся, чтение работает, новый запрос сохраняет паузу, явный resume разрешает commit. На настоящем OMP 18.1.5 команды pause, просмотр состояния и resume исполнились через RPC без единого agent_start. Пометка чтения ADR проверена с сохранением исходного результата и отсутствием изменений у обычных файлов и ошибок чтения.

Диагностика `/huimem context` проверена на окончательно обрезанном блоке: совпадение размера, исключение неполного JSON и похожего текста из канонического preview, версии записей, подавление последовательного дубля и предел 100 записей. Это проверка метаданных подготовленного контекста; использование фактов моделью она не доказывает.

Проверяются версии и история исправлений, отклонение неверных источников, устаревание, восстановление после открытия базы, конфликты версий, ошибки доступа, размер контекста, архитектурные запреты и отсутствие принудительных циклов продолжения.

Команда `bun run check` также проверяет manifest, наличие семи skills и отсутствие данных runtime в starter. GitHub CI проверяет, что сборка соответствует исходникам.

## Проверка с локальной моделью

**Атрибуция уровня утверждения, 2026-09-11: сценарий пройден.** Пометка при чтении ADR
адресует теперь блок основания, а не файл целиком. Та же неизменённая фикстура
`audit/legacy-evidence-20260910/project`, те же два вопроса из `check-legacy-evidence.py`,
свежая сборка.

| Прогон | Прежде | Теперь |
| --- | --- | --- |
| «сколько попыток и почему» | не пройден, 20,9 с: пользователю приписано «поэтому большее число попыток попадает в дедупликацию» | пройден: приведена цитата пользователя, дописанной связи нет |
| вопрос-провокация | оба утверждения помечены unverified, но записан пустой checkpoint вопреки запрету | пройден, 167 с: записи нет; «связка большее число → дедуп — это глосс ADR, не пользовательская формулировка» |
| повтор на свежей копии | — | пройден, 33 с: раздел «Граница подтверждения» добавлен моделью без отдельной просьбы |

Целостность проверена в обоих проектах: `versions` 1, `checkpoints` 2 — **оба из фикстуры**,
метки времени совпадают до миллисекунды; хеши `0001-max-retry-attempts.md`, `MEMORY.md` и
`RECORDS.md` равны исходным. Фикстура не правилась.

Механизм объясняет результат: у старого документа нет блока основания, поэтому цитируемой
части у него нет вовсе, и статус «принято» ничего не подтверждает. Прежняя пометка
сообщала «это текст документа», новая — «подтверждённого основания здесь нет».

**Чего этот результат не доказывает.** Три прогона на одной фикстуре и одной модели —
не оценка надёжности. Не проверены: поведение после сжатия, чтение документа в обход
инструмента `read` (`grep`, shell), другие модели, документ с блоком основания и
искажённой внутри него цитатой. Разница во времени между прогонами — отдельные замеры,
а не показатель производительности.

**Кандидат с пометкой при чтении ADR, 2026-09-10:** тот же неизменённый старый ADR, свежая сессия локальной Qwen. Обычный ответ (17,6 с) процитировал оригинальное решение без добавленной причины; уточнение (71,6 с) явно отделило оба неподтверждённых объяснения от цитаты пользователя. Пометка DOCUMENT_PROVENANCE присутствует в реальном результате инструмента. Commit не вызывался, осталось по одной версии и checkpoint, Markdown совпал по хешам. Пауза в этом прогоне не включалась: команды pause/resume проверены отдельно без модели, принудительное отклонение commit — тестами адаптера. Это один успешный сценарий, не доказательство общей надёжности; сжатие и обходные способы чтения этим прогоном не проверены.

**Повтор с выдачей исходных свидетельств, 2026-09-10:** исключили пересказ решения из поисковой выдачи и текст ADR из автоматического preview, исправили приоритет источников. На том же старом ADR обычный ответ за 20,9 с снова приписал пользователю неподтверждённое объяснение. При уточнении за 57,2 с модель назвала два спорных утверждения unverified, но записала пустой checkpoint вопреки просьбе ничего не сохранять. Markdown остался неизменным. Диагностика подтвердила полное включение исходной записи v1, без обрезки (4297 символов первоначального блока). Это локализует сбой после подготовки выдачи, но не доказывает получение всех данных провайдером. Регрессия в целом не пройдена; ускорение одного повтора не является оценкой производительности.

При прямом уточнении того же сценария модель за 100,6 с подтвердила оба неподтверждённых объяснения, сославшись на статус ADR «принято». Исходные Markdown-файлы до и после прогона совпали по хешам. Таким образом, проблема воспроизвелась и при обычном воспоминании, и при запросе основания.

**Регрессия со старым ADR, 2026-09-10:** в новой сессии оставили исходный документ, где модель ранее дописала неподтверждённую причинную связь, и сохранили исходное пользовательское свидетельство в SQLite. Ответ восстановил 7 попыток, Берилл-42 и 19 минут, но снова добавил: «при большем числе попыток они попадают в дедупликацию». Первый ответ занял 580,8 с; причину задержки отдельно не установили. Защита от домыслов в старых канонических документах эту проверку **не прошла**. Новая диагностика контекста добавлена после запуска этого процесса и им не проверялась.

Окружение: Windows, OMP 18.1.5, Bun 1.4.0, локальная Huihui-Qwen3.8-27B-abliterated-UD-Q4_K_XL.gguf, доступный контекст 65536. Облачный провайдер не использовался.

Четыре отдельных процесса OMP использовали одну проектную память без общей истории сессии:

| Сценарий | Результат | Время | Вызовы модели | Выходные токены |
| --- | --- | ---: | ---: | ---: |
| Сохранить PostgreSQL 17 и Stripe | Сохранено; две ошибки валидации исправлены моделью | 73,7 с | 8 | 2224 |
| Вспомнить в новой сессии | Правильный ответ | 6,0 с | 1 | 120 |
| Заменить Stripe на YooKassa | Та же запись, версия 2 | 69,8 с | 4 | 2396 |
| Вспомнить после замены | Правильный провайдер и причина | 5,7 с | 1 | 132 |

Это один успешный прогон, а не статистическая оценка надёжности. Для Stripe записанная причина повторяла выбор провайдера: проверка цитаты прошла, но полноценного объяснения «почему» это не доказывает.

Прямая загрузка собранного расширения и вызов `project_memory status` также прошли на реальной модели. Пользовательские диалоги и рабочая база в репозиторий не включены.

## Установка из GitHub и прогон на живом коде (2026-09-08 / 09)

`omp plugin install github:Clientik/omp-huimem#v0.1.0` прошла целиком: плагин установился, зарегистрировался в `omp-plugins.lock.json` и отработал живые сессии. Ранее этот путь был помечен как непроверенный.

Подопытный проект — реальный TypeScript-репозиторий на 44 файла (исходники самого плагина), а не синтетический пример.

| Сценарий | Время | Результат |
| --- | ---: | --- |
| `initmem`: карта проекта по живому коду | ~9,5 мин | все числа сверены вручную: размеры файлов, число строк, состав тестов, размер bundle — совпали |
| Навигация в новой сессии | 27 с | четыре ссылки вида `файл:строка` проверены, все точные |
| Правка документа при активном нарушении архитектуры | 130 с | нарушение названо, код **не** изменён ради прохождения проверки, запись сделана без напоминания |

В карту попал реальный отказ сборки (`bun run check`, ENOENT), и агент доложил его как отказ, а не как успех.

Архитектурные правила проверены на созданном нарушении, а не «в принципе»: чистое состояние `ok: true`, после внесения `bun:sqlite` в адаптер и фонового таймера — оба правила сработали с именем файла и причиной, после отката снова `ok: true`.

Цикла повторных чекпоинтов нет: три сессии — три отдельных прогона, 19 эпизодов, один чекпоинт.

**Что этот прогон показал слабым:** `MEMORY.md` и `todo.json` за три сессии остались незаполненными. Стенограмма ведётся автоматически, реестр модель пополняет сама, а человекочитаемый слой требует явной просьбы.

## Явное включение по проекту

Замерено на голом каталоге: расширение создавало `.memory/runtime/state.sqlite` в любом проекте, где запущен OMP, записывало эпизоды и выдавало `ARCHITECTURE_UNCONFIGURED` — при `versions=0, checkpoints=0`, поскольку писать было некуда. В чужом репозитории такая база не покрыта `.gitignore`.

С версии 0.2.0 признак включения — `.memory/MEMORY.md`. Проверено живыми сессиями: в каталоге без него после сессии не осталось ни одного файла, кроме исходного `main.py`, и вывод чист; в развёрнутом проекте контекст и запись эпизодов работают по-прежнему. Каталог `.memory/`, оставшийся от прежних самозапусков, включением не считается.

## Автозапись и реальное сжатие (2026-09-09)

Один изолированный прогон v0.3.0 на той же локальной Qwen: обычная просьба изменить константу с указанием причины, без «запомни», привела к обновлению MEMORY.md, созданию ADR и записи решения в SQLite. Снимок снят до вопроса на воспоминание: одна версия и один checkpoint.

Затем выполнено настоящее ручное сжатие через RPC OMP. Для короткого теста только в тестовом проекте `keepRecentTokens` уменьшен до 512: активная история сократилась с 16 сообщений до 3, исходный запрос заменён сводкой. Это не проверка автоматического сжатия при заполнении окна. И после сжатия, и в новой сессии модель восстановила три исходных значения.

**Выявлен дефект качества:** модель добавила в принятый ADR неподтверждённые последствия и оценку альтернатив. Они перешли в сводку сжатия и в оба последующих ответа. Точная цитата в реестре была корректна, но она не проверяет свободный текст ADR. Поэтому этот прогон подтверждает сохранение и перенос фактов, одновременно показывая отсутствие полной защиты от закрепления домыслов.

Проверка использовала starter, один явно загруженный bundle и отключённые skills. Один прогон не доказывает устойчивую автозапись; результат после сжатия зависит также от нативной сводки OMP.

## Что ещё нужно проверить

### Восстанавливаемое представление реестра (10 сентября, рабочая версия)

Unit-тесты проверяют создание RECORDS.md, замену старого значения, сохранность ручных заметок, конфликт ручной правки, отказ публикации с сохранённым checkpoint, восстановление при открытии и после записи файла до подтверждения, миграцию схемы 1→2 и запрет ссылаться на генерируемый файл как на первичное свидетельство. Аварийные состояния моделируются тестами; это не испытание отключения питания.

Живой прогон на локальной Qwen: обычная задача без «запомни» за 73,6 с создала одну версию, один checkpoint и автоматически опубликованный RECORDS.md (`synced`). Модель отдельно записала ADR; MEMORY.md и todo.json остались без изменений. После настоящего ручного сжатия (12161 токен до сжатия) и в новой сессии ответы за 6,4 и 9,4 с сохранили 7 попыток, Берилл-42 и 19 минут без прежнего неподтверждённого объяснения. В сводке этого прогона причина тоже осталась точной цитатой. Это один успешный повтор, не гарантия надёжности или улучшения скорости.

Приоритеты развития и датированные первоисточники: [план развития памяти](ROADMAP.md).

В расширение добавлен `session.compacting`: он передаёт штатному суммаризатору короткое правило сохранять происхождение утверждений. Unit-тесты исходников и bundle проверяют работу до первого запроса после resume и отсутствие результата без маркера включения. Предварительная живая проба на OMP 18.1.5 и локальной Qwen подтвердила доставку context по уникальному маркеру в сводке, но не полное устранение домыслов. Финальный хук не содержит тестового маркера. Автоматический путь проверен по исходникам OMP, не отдельным живым прогоном.

После уточнения правил происхождения и разделения цитаты/интерпретации в выдаче повторён тот же локальный сценарий. Решение автоматически попало в SQLite; после сжатия и в новой сессии ответы сохранили исходные значения без прежнего неподтверждённого объяснения. Но нативная сводка OMP снова содержала домысел, а MEMORY.md и ADR в этом прогоне не обновились. Это ограниченное улучшение ответов, не полное устранение ошибки и не доказательство стабильного заполнения файлов.

- Повторяемость на разных моделях и более длинных задачах.
- Автоматическое сжатие при заполнении контекста, повторяемость воспоминания и отсутствие добавленных моделью утверждений.
- Прерывания и аварии процесса OMP во время работы.
- Параллельные сессии OMP и работа с ветками Git.
- Полный расход входных и выходных токенов относительно работы без плагина.
- Установка из GitHub на чистом окружении.

Отдельные unit-тесты конфликтов и восстановления базы не заменяют эти проверки целого OMP. Гарантий идеальной памяти, полной защиты архитектуры и фиксированной экономии токенов пока нет.
